import { Button } from "@/components/ui/button";
import { Typography } from "@/components/ui/typography";

const HomePage = () => {
  return (
    <div>
      <Typography tag="h1" variant="heading-md">Hello world</Typography>
      <Button>Button</Button>
    </div>
  );
};

export default HomePage;
